name: truvari_remap
channels:
  - conda-forge
  - bioconda
dependencies:
  - bcftools==1.14
  - trf==4.09.1
  - mafft>=7.515
  - pywfa >=0.5.1
  - repeatmasker==4.2.3
  - edlib>=1.3.9
  - Truvari==4.3
  - pip
  - pip:
    # PyPI bwapy 0.1.4 rejects `-a` (the option Truvari 4.3 passes to BwaAligner).
    # ACEnglish/bwapy master adds `-a` support; see truvari issue #173.
    - git+https://github.com/ACEnglish/bwapy.git@e56d295d
