variables:
  # Use libmamba solver for faster conda operations
  CONDA_SOLVER: libmamba

cache:
  # Share cache across branches since conda envs are deterministic by hash
  key: conda-envs-shared
  paths:
    - .snakemake/conda/

before_script:
  - source /home/gitlab-runner/miniforge3/etc/profile.d/conda.sh
  - conda activate defrabb-ci
  - echo "Pipeline tools (from pre-built defrabb-ci env)"
  - mamba --version
  - snakemake --version
  - snakefmt --version
  - black --version
  - pytest --version
  - apptainer --version

# lint-test:
#   artifacts:
#     paths:
#         - lint
#   script:
#     - snakemake --lint

smkformat:
  script:
    - echo "Running snakefmt"
    - snakefmt .

pytest:
  script:
    - echo "Running pytest"
    - pytest .tests

run-test:
  timeout: 30m
  artifacts:
    paths:
        - smalltest
    when: always
  script:
    - snakemake -p --use-conda --conda-frontend mamba --use-apptainer --cores 1 --verbose --config "_happy_threads=1"
